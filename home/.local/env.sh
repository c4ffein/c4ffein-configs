alias gr=grep
alias la="ls -lah"
alias p="python -c"

# Maybe useless past this line, add as see fit

# activate-node-modules() {  # safe with both .bashrc and .zshrc
#   export PATH="$PATH:$(printf '%q' "$(pwd)")/node_modules/.bin"
# }
